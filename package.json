{
  "name": "project-carlo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "db:drop": "uv run python -m pdf_extract db drop",
    "db:create": "uv run python -m pdf_extract db create",
    "detect": "uv run python -m pdf_extract detect run",
    "detect:apply": "uv run python -m pdf_extract detect apply",
    "fetch": "uv run python -m pdf_extract fetch",
    "process": "uv run --env-file .env python -m pdf_extract process",
    "extract": "uv run --env-file .env python -m pdf_extract extract",
    "compare": "uv run python -m pdf_extract compare",
    "verify": "uv run --env-file .env python -m pdf_extract verify run",
    "verify:apply": "uv run python -m pdf_extract verify apply",
    "geocode": "uv run python -m pdf_extract geocode run",
    "geocode:apply": "uv run python -m pdf_extract geocode apply",
    "test": "uv run pytest",
    "lint": "uv run ruff check .",
    "extract:web": "pnpm run db:create && uv run python -m pdf_extract.extract_frontend_db",
    "verify:frontend-snapshot": "uv run python -m pdf_extract verify-frontend-snapshot",
    "weekly:run": "pnpm fetch && pnpm process && pnpm extract:web && pnpm verify:frontend-snapshot && pnpm build:web",
    "install:web": "pnpm install",
    "dev:web": "pnpm --filter web dev",
    "build:web": "pnpm --filter web build",
    "preview:web": "pnpm --filter web preview",
    "lint:web": "pnpm --filter web lint",
    "check:web": "pnpm --filter web check"
  }
}
